source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gemspec
gem 'pry-byebug'
gem 'power_assert', git: 'https://github.com/sonots/power_assert', branch: 'close_file'
